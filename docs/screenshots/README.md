# Screenshot evidence

The workflow result links in [`../evidence.md`](../evidence.md) are the authoritative evidence because they are publicly viewable and include the repository address bar.

The required frontend verification screenshot must show the Movie List page loaded from the
frontend Kubernetes LoadBalancer URL. A localhost screenshot is not deployment evidence.
